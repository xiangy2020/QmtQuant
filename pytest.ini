[pytest]
# 测试根目录（从 QmtQuant 下执行）
testpaths = tests

# 测试文件匹配规则
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 将项目根目录加入 Python 路径，使 import 正常工作
pythonpath = .

# 输出格式
addopts = -v --tb=short
